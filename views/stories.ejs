
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stayurt - Story Showcase</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .story-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transform-style: preserve-3d;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 
                0 10px 30px rgba(0, 0, 0, 0.1),
                0 1px 8px rgba(0, 0, 0, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.8);
        }

        .story-card:hover {
            transform: translateY(-15px) rotateX(5deg) rotateY(5deg) scale(1.02);
            box-shadow: 
                0 25px 50px rgba(0, 0, 0, 0.2),
                0 10px 30px rgba(102, 126, 234, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.9);
        }

        .story-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0) 100%);
            border-radius: inherit;
            pointer-events: none;
        }

        .floating-animation {
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(1deg); }
            50% { transform: translateY(-5px) rotate(0deg); }
            75% { transform: translateY(-15px) rotate(-1deg); }
        }

        .logo-3d {
            text-shadow: 
                0 1px 0 rgba(255, 255, 255, 0.8),
                0 2px 0 rgba(255, 255, 255, 0.6),
                0 3px 0 rgba(255, 255, 255, 0.4),
                0 4px 0 rgba(255, 255, 255, 0.2),
                0 5px 10px rgba(0, 0, 0, 0.3);
            transform: perspective(500px) rotateX(15deg);
        }

        .gradient-text {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .add-icon {
            transition: all 0.4s ease;
            filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
        }

        .story-card:hover .add-icon {
            transform: scale(1.2) rotateZ(10deg);
            filter: drop-shadow(0 8px 16px rgba(102, 126, 234, 0.3));
        }

        .parallax-bg {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        .neon-glow {
            box-shadow: 
                0 0 20px rgba(102, 126, 234, 0.5),
                0 0 40px rgba(102, 126, 234, 0.3),
                0 0 60px rgba(102, 126, 234, 0.1);
        }

        .card-grid {
            perspective: 1000px;
        }

        .featured-card {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            transform-style: preserve-3d;
            transition: all 0.4s ease;
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.1),
                0 10px 20px rgba(102, 126, 234, 0.2);
        }

.featured-card:hover {
            transform: translateY(-10px) rotateX(2deg);
            box-shadow: 
                0 30px 60px rgba(0, 0, 0, 0.15),
                0 15px 30px rgba(102, 126, 234, 0.3);
        }

        .pulse-animation {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Floating Background Elements -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-32 h-32 bg-white opacity-10 rounded-full floating-animation" style="animation-delay: 0s;"></div>
        <div class="absolute top-40 right-20 w-24 h-24 bg-white opacity-10 rounded-full floating-animation" style="animation-delay: 2s;"></div>
        <div class="absolute bottom-40 left-1/4 w-40 h-40 bg-white opacity-10 rounded-full floating-animation" style="animation-delay: 4s;"></div>
        <div class="absolute bottom-20 right-1/3 w-28 h-28 bg-white opacity-10 rounded-full floating-animation" style="animation-delay: 1s;"></div>
    </div>

    <!-- Header -->
    <header class="relative py-20 text-center">
        <div class="max-w-7xl mx-auto px-6">
            <h1 class="logo-3d text-8xl font-black text-white mb-6">STAYURT</h1>
            <p class="text-2xl text-white opacity-90 max-w-3xl mx-auto font-light">
                Where Stories Come to Life in Three Dimensions
            </p>
            <div class="mt-8">
                <div class="glass-effect inline-block px-8 py-4 rounded-full">
                    <span class="text-white font-medium">✨ Immersive Story Experience ✨</span>
                </div>
            </div>
        </div>
    </header>

    <!-- Featured Story Section -->
<% story.forEach(function(elem){ %>
    <section class="max-w-7xl mx-auto px-6 py-16">
        <h2 class="text-4xl font-bold text-white mb-12 text-center"></h2>
        <div class="featured-card rounded-3xl p-10 max-w-5xl mx-auto">
            <div class="flex flex-col lg:flex-row items-center gap-10">
                <div class="w-full lg:w-2/5">
                    <div class="aspect-square bg-gradient-to-br from-purple-100 via-blue-100 to-pink-100 rounded-2xl flex items-center justify-center border-2 dashed border-purple-300 neon-glow">
                        <div class="text-center">
                            <div class="text-8xl mb-6 pulse-animation"><img src="/images/uploads/<%= elem.cover %>" alt=""></div>
                            <p class="gradient-text font-bold text-xl">Featured Story</p>
                        </div>
                    </div>
                </div>
                <div class="w-full lg:w-3/5">
                    <h3 class="text-3xl font-bold gradient-text mb-6"><%= elem.title %></h3>
                    <p class="text-gray-700 mb-8 leading-relaxed text-lg"><%= elem.theme %></p>
                    <div class="flex items-center gap-6">
                        <div class="w-16 h-16 bg-gradient-to-r from-purple-500 to-blue-500 rounded-full flex items-center justify-center neon-glow">
                            <span class="text-white font-bold text-xl">S</span>
                        </div>
                        <div>
                            <p class="font-bold text-gray-800 text-lg">Stayurt</p>
                            <a href="/stories/<%=elem.title%>" class="text-gray-600">Read</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
     <% }); %>



    
    <!-- Footer -->
    <footer class="glass-effect py-16 mt-20">
        <div class="max-w-7xl mx-auto px-6 text-center">
            <h3 class="text-3xl font-bold text-white mb-6">STAYURT</h3>
            <p class="text-white opacity-80 mb-8 text-lg">Where Every Story Finds Its Perfect Stage</p>
            <div class="flex justify-center space-x-8 text-white">
                <a href="/about" class="hover:text-purple-300 transition-colors font-medium">About</a>


<a href="/" class="hover:text-purple-300 transition-colors font-medium">home</a>
                <a href="/contact" class="hover:text-purple-300 transition-colors font-medium">Contact</a>
            </div>
        </div>
    </footer>
</html>